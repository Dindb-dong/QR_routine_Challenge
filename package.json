{
  "name": "qr_challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^8.11.0",
    "ajv-keywords": "^5.1.0",
    "cra-template-pwa": "2.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1",
    "web-push": "^3.6.7",
    "web-vitals": "^5.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "npx serve -s build -l 3000",
    "serve-test": "node serve-test.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "react": {
    "version": "18.2.0"
  },
  "react-dom": {
    "version": "18.2.0"
  },
  "devDependencies": {
    "express": "^5.1.0"
  }
}
